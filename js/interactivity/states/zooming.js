import { domLog } from "../../main";
var Zooming = /** @class */ (function () {
    function Zooming() {
        domLog("Zooming");
    }
    Zooming.prototype.update = function (manager, action) { };
    return Zooming;
}());
